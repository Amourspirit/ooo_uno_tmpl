#from _base_const import BaseConst
#extends BaseConst
#implements respond
$self.init_data()#slurp
#attr $sort = True ## set is const_dict is sorted
#attr $hex = False
#attr $flags = False
#attr $name = ""
#attr $namespace = ""
#attr $quote = set()
#attr $typings = set()
#attr $desc = "" ## str or list or tupple
#attr $imports = []
## $from_imports is a tuple eg: $from_imports = [('.tab_align', 'TabAlign')]
#attr $from_imports = []
#attr $link = ""
## $const_dict, Comment can be string or list or tupple eg:
#*
$const_dict = {
    "NONE": ("0", "No column or page break is applied."),
}
*#
#attr $const_dict = {}
$self.load_data()#slurp
## Main Template
#set $base_class = 'ConstIntFlagsBase' if $flags else 'ConstIntBase'
#set $key_list = list($const_dict.keys())
# coding: utf-8
# this is a auto generated file generated by Cheetah
#for $imp in $imports
import $imp
#end for
#for $frm, $imp in $from_imports
from $frm import $imp
#end for
from ...base.const import $base_class


class ${name}($base_class):
    """
    #if isinstance($desc, str)
    $desc
    #else
        #for $line in $desc
    $line
        #end for
    #end if
#if $link:

    See Also:
        `API $name <$link>`_
#end if
    """
#for $key in $key_list
#set $value = $const_dict[$key]
    #if $hex
    $key = int('${value[0]}', 16)
    #else
    $key = $value[0]
    #end if
    #if len($value) > 1
    """
        #if isinstance($value[1], str)
    ${$value[1]}
        #else
            #for $line in $value[1]
    $line
            #end for
        #end if
    """
    #end if
#end for