#from _base_const import BaseConst
#extends BaseConst
#implements respond
$self.init_data()#slurp
#attr $sort = True ## set is const_dict is sorted
#attr $hex = False
#attr $flags = False
#attr $name = ""
#attr $namespace = ""
#attr $link = ""
#attr $base_class = ""
#attr $quote = set()
#attr $typings = set()
#attr $desc = "" ## str or list or tupple
#attr $imports = []
#attr $from_imports = []
#attr $from_typing_imports = []
#attr $requires_typing = False
#attr $const_dict = {}
$self.load_data()#slurp
## Main Template
#set $key_list = list($const_dict.keys())
# coding: utf-8
# this is a auto generated file generated by Cheetah
#for $imp in $imports
import $imp
#end for
#for $frm, $imp in $from_imports
from $frm import $imp
#end for
#for $frm, $imp in $from_typing_imports
from $frm import $imp
#end for


class ${name}($base_class):
    """
    #if isinstance($desc, str)
    $desc
    #else
        #for $line in $desc
    $line
        #end for
    #end if
#if $link:

    See Also:
        `API $name <$link>`_
#end if
    """
#for $key in $key_list
#set $value = $const_dict[$key]
    #if $hex
    $key = int('${value[0]}', 16)
    #else
    $key = $self.get_q_type($value[0])
    #end if
    #if len($value) > 1
    """
        #if isinstance($value[1], str)
    ${$value[1]}
        #else
            #for $line in $value[1]
    $line
            #end for
        #end if
    """
    #end if
#end for