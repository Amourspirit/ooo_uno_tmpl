#from _base_struct import BaseStruct
#import re
#extends BaseStruct
#implements respond
$self.init_data()#slurp
#attr $dynamic_struct = False
#attr $sort = True ## set is attribs is sorted
#attr $name = ""
#attr $namespace = ''
#attr $allow_db = True
#attr $libre_office_ver = False
#attr $desc = ""
#attr $include_desc = True
#attr $link = ""
#attr $requires_typing = False
#attr $inherits = []
#attr $imports = []
#attr $extends_map = {}
#attr $from_imports = []
#attr $from_imports_typing = []
#attr $quote = set()
#attr $typings = set()
#attr $attribs = []
#attr $fullname = ''
$self.load_data()#slurp
## Main Template
#set $re_underscore_hex = r"(_[0-9A-Fa-f]*)"
#set $safe_name = $self.get_safe_word($name)
#set $sorted = $self.get_sorted_names()
#set $is_parent = $self.has_uno_extends()
# coding: utf-8
#include raw "resources/inc_lic.txt"
# Struct Class
# this is a auto generated file generated by Cheetah
# Namespace: $namespace
#if $libre_office_ver:
# Libre Office Version: $libre_office_ver
#end if
#if len($sorted) > 0
from ooo.oenv import UNO_NONE
#end if
#for $imp in $imports
import $imp
#end for
#for $imp in $from_imports
$self.get_from_import($name, $imp)
#end for
#if $requires_typing or len($sorted) > 0:
import typing
#end if
#if len($from_imports_typing) > 0:
## if typing.TYPE_CHECKING:
#for $imp in $from_imports_typing
$self.get_from_import($name, $imp)
#end for
#end if


#if $allow_db:
class ${safe_name}($self.get_class_inherits_from_db()):
#else
class ${safe_name}($self.get_class_inherits($name, $inherits)):
#end if
#if $include_desc
    """
    Struct Class

    #if isinstance($desc, str)
    $desc
    #else
        #for $line in $desc
    $line
        #end for
    #end if
    #if $link:

    See Also:
        `API $name <$link>`_
    #end if
    """
#end if
    __ooo_ns__: str = '$namespace'
    __ooo_full_ns__: str = '$fullname'
    __ooo_type_name__: str = 'struct'
## typeName: Literal requires python >= 3.8
    typeName: str = '$fullname'
    """Literal Constant ``$fullname``"""

#set $_, $first_index = $sorted[0]
#set $first_itm = $attribs[$first_index]
#set $first_itm_name = $self.get_safe_word($first_itm['name'])
    $self.get_constructor()
        """
        Constructor

        Other Arguments:
            ``$first_itm_name`` can be another ``$safe_name`` instance,
                in which case other named args are ignored.
#if is_parent
                However ``**kwargs`` are still passed so parent class.
#end if

        Arguments:
#for $i, $tpl in enumerate($sorted)
#set $index = tpl[1]
#set $itm = $attribs[$index]
#set $itm_name = $self.get_safe_word($itm['name'])
#set $itm_type = $itm.type.removeprefix('typing.')
#set $itm_type = re.sub(re_underscore_hex, '', $itm_type, 1)
            $itm_name ($itm_type, optional): $itm_name value
#end for
        """
#if $self.has_uno_extends()
        super().__init__(**kwargs)
#end if
        if isinstance($first_itm_name, $safe_name):
            oth: $safe_name = $first_itm_name
#for $i, $tpl in enumerate($sorted)
#set $index = tpl[1]
#set $itm = $attribs[$index]
#set $itm_name = $self.get_safe_word($itm['name'])
#set $attrib_default = $self.get_attrib_default($itm)
            self._$self.camel_to_snake($itm_name) = oth.$itm_name
#end for
            return
        else:
#for $i, $tpl in enumerate($sorted)
#set $index = tpl[1]
#set $itm = $attribs[$index]
#set $itm_name = $self.get_safe_word($itm['name'])
#set $attrib_default = $self.get_attrib_default($itm, True)
    #if attrib_default == 'UNO_NONE'
            if $itm_name is UNO_NONE:
                self._$self.camel_to_snake($itm_name) = $self.get_attrib_default($itm)
            else:
                self._$self.camel_to_snake($itm_name) = $itm_name
    #else
            self._$self.camel_to_snake($itm_name) = $itm_name
    #end if
#end for


#for $i, $tpl in enumerate($sorted)
#set $index = tpl[1]
#set $itm = $self.get_attrib_for_prop($index)
#set $itm_name = $self.get_safe_word($itm['name'])

    @property
    def ${itm_name}(self) -> $itm['type']:
#if $include_desc and $itm['desc']
        """
    #if isinstance($itm['desc'], str):
        $itm['desc']
    #else
        #for $line in $itm['desc']
        $line
        #end for
    #end if
        """
#end if
        return self._$self.camel_to_snake($itm_name)
    
    @${itm_name}.setter
    def ${itm_name}(self, value: $itm['type']) -> None:
        self._$self.camel_to_snake($itm_name) = value
#end for


__all__ = ['$safe_name']