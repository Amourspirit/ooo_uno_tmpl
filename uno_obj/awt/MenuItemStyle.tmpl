#from _const_base import const_base as c_base
#extends c_base
#attr $hex = False
#attr $sort = True ## set is const_dict is sorted
#attr $flags = True
#attr $name = "MenuItemStyle"
#attr $desc = "These values are used to specify the properties of a menu item."
#attr $imports = []
## $from_imports is a tuple eg: $from_imports = [('.tab_align', 'TabAlign')]
#attr $from_imports = []
#attr $link = "https://api.libreoffice.org/docs/idl/ref/namespacecom_1_1sun_1_1star_1_1awt_1_1MenuItemStyle.html"
## $const_dict, Comment can be string or list or tupple eg:
#*
$const_dict = {
    "NONE": ("0", "No column or page break is applied."),
}
*#
#attr $const_dict = {
    "CHECKABLE": ("1", [
        "specifies an item which can be checked independently."
    ]),
    "RADIOCHECK": ("2", [
        "specifies an item which can be checked dependent on the neighbouring items."
    ]),
    "AUTOCHECK": ("4", [
        "specifies to check this item automatically on select."
    ])
}