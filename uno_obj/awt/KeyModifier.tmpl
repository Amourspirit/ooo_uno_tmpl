#from _const_base import const_base as c_base
#extends c_base
#attr $hex = False
#attr $sort = True ## set is const_dict is sorted
#attr $flags = True
#attr $name = "KeyModifier"
#attr $desc = 'These values are used to specify which modifier keys are pressed.'
#attr $imports = []
## $from_imports is a tuple eg: $from_imports = [('.tab_align', 'TabAlign')]
#attr $from_imports = []
#attr $link = "https://api.libreoffice.org/docs/idl/ref/namespacecom_1_1sun_1_1star_1_1awt_1_1KeyModifier.html"
## $const_dict, Comment can be string or list or tupple eg:
#*
$const_dict = {
    "NONE": ("0", "No column or page break is applied."),
}
*#
#attr $const_dict = {
    "SHIFT": ('1', 'refers to both shift keys.'),
    "MOD1": ('2', 'refers in the most cases to the "Ctrl" key (Cmd on macOS).'),
    "MOD2": ('4', 'refers in the most cases to the "Alt" key.'),
    "MOD3": ('8', 'refers in the most cases to the "Ctrl" key (macOS)')
}